[project]
name = "timetable"
version = "0.1.0"
description = "Парсинг преподавателей «Проектная деятельность» по группам РУТ (МИИТ)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "openpyxl>=3.1.0",
]

[project.scripts]
timetable = "main:main"
